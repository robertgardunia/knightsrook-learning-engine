# --- Client (Babylon+Vite dev server) ---
CLIENT_PORT=5320

# --- Retrieval service (FastAPI) ---
RETRIEVAL_PORT=5110
API_KEY=

# --- Postgres (pgvector + AGE) ---
POSTGRES_USER=
POSTGRES_PASS=
POSTGRES_DB=learning_engine
POSTGRES_HOST=db
POSTGRES_PORT=5432

# --- xAPI relay (tsn-node-kit's XApiRelay, reused from garage's relay-server.js pattern) ---
XAPI_RELAY_PORT=5040
VITE_XAPI_SOCKET_URL=http://localhost:5040
LRS_ENDPOINT=
LRS_KEY=
LRS_SECRET=

# --- tsn-xapi-handler (actor migration + LRS reporting query — separate deployed service, not a dependency of this repo) ---
XAPI_HANDLER_URL=
XAPI_HANDLER_TOKEN=

# --- ElevenLabs (per-theme avatar voice agents) ---
ELEVENLABS_API_KEY=
